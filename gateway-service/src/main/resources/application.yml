server:
  port: 8080

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      server:
        webflux:
          routes:
            #系统服务
            - id: system-service
              uri: http://localhost:8091
              predicates:
                - Path=/api/system/**
              filters:
                - StripPrefix=1
            #文件服务
            - id: file-service
              uri: http://localhost:8092
              predicates:
                - Path=/api/file/**
              filters:
                - StripPrefix=1

#token相关配置
token:
  secret: my-secret-key
  headerName: token
  expireTime: 3600

#白名单配置
security:
  ignore:
    whites:
      - /api/user/login
      - /api/user/register